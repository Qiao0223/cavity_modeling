{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run current file as module",
            "type": "debugpy",
            "request": "launch",
            "module": "${relativeFileDirname}.${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
            "justMyCode": true
        }
    ]
}
